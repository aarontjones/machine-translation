1) Add a clear field button to input text, which clears the input field and sets the output field back to empty.

2) Use Websockets to get realtime translation information

3) Allow txt files to be inputs and outputs, overriding if there is text in the input field, and only output regarding if its a txt file.

4) Allow Differenet languages